# How to Solve RSA Challenge

1. Inspect public key
   openssl rsa -pubin -in public.pem -text -noout

2. Check ciphertext size (128 bytes → RSA-1024)

3. Decrypt using private key
   openssl pkeyutl -decrypt -inkey private.pem -in cipher.bin -out flag.txt

4. Final FLAG:
   UNAME{DyNaMiTE007}
